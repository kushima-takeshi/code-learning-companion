# 要件定義

## 1. プロダクトの目的

AIが書いた、またはAIの補助で書いたコードを「動くが説明できないコード」で終わらせず、開発者自身が説明・判断・応用できる技術知識に変える。

その場でAIに質問して理解したつもりでも、後から思い出せない・似た記法を混同する、という課題を解決する。

## 2. 対象ユーザー

- CursorなどのAIコーディング支援を日常的に使う個人開発者・学習中の開発者
- AIの説明を受けながら実装は進められるが、知識が断片化していると感じる人
- 公開されているPRや実装の判断を、後から学習材料として振り返りたい人

## 3. 提供する価値

- 実際のPRとAIとの会話を、自分専用の教材に変える
- 自分がつまずいた箇所から、短い復習問題を作る
- 回答・苦手理由・深掘り結果を残し、時間を置いて再確認する
- なぜその実装や設計判断を選んだのかを、根拠付きで振り返れる

## 4. 基本体験

1. ユーザーがGitHubでログインする。ログインは学習データをユーザーごとに保存するために行う。
2. ユーザーが学習対象の公開PRのURLを入力する。必要ならCursor会話をMarkdownでアップロードする。
3. アプリが選択したPR、関連コード、取り込んだCursor会話を解析する。LLMがPRに含まれる学習候補を提案し、各候補には根拠となるPR・コード・会話箇所を表示する。
4. ユーザーが候補を選び、選択肢、説明、小さなコード修正の問題に回答する。
5. 回答と苦手理由を保存し、フィードバックを受ける。
6. 必要なら同じ文脈から深掘り対話を行い、学習要点を保存する。
7. 後日、同じPRのスナップショットを根拠に、別の問い方で再出題する。

### MVPの入口

MVPでは「指定した公開PRを教材として学ぶ」を採用する。公開PRは、自分が作成したものか他者が作成したものかを問わず、URLで指定できる。

    公開PRのURLを入力する
    → 必要ならCursor会話Markdownをアップロード
    → 解析する
    → 問題に答える

開発セッションの開始・終了、開発中の自動収集、復習開始時に問題を事前準備する体験は後続機能とする。

### 公開PRの取り込み仕様

- ユーザーは公開PRのURLを入力するだけで教材を作成できる
- 初回取得時に、PR本文、差分、変更ファイル、コミット情報、レビューコメントを取得する
- 非公開PR、存在しないPR、取得できないPRのURLが入力された場合は、公開PRのURLを入力するようエラーメッセージを表示する

#### 周辺コードの取得

PR差分だけで問題の根拠が十分な場合は、周辺コードを追加取得しない。必要な場合だけ、以下の順に範囲を広げる。

1. 変更箇所の前後のコード
2. 同じファイル内で直接参照される型・関数・定数の定義
3. それでも判断できない場合だけ、呼び出し元などの関連ファイル

リポジトリ全体を一律に取得・解析しない。取得範囲は固定の行数ではなく、問題の根拠を理解するための最小限とする。

## 5. 問題の種類

### 学習候補の絞り込み

PRの解析時、LLMは複数の学習候補を検出してよい。ただし、最初の画面に表示する候補は優先度の高い最大3件に絞る。

優先度は、PRにおける変更の中心度、取り込んだCursor会話での質問、過去の苦手理由、未学習・未定着かどうか、直近の復習履歴を材料に決める。

ユーザーは候補から、その日に学ぶものを1つ選ぶ。残りの候補を同じ回に解くことは強制しない。

### 選んだ候補の問題セット

選んだ1候補には、原則として次の3問をこの順で出す。

1. 選択肢問題: 記法やコードの挙動を確認する
2. 自由記述問題: 必要な理由や違いを自分の言葉で説明する
3. 小さなコード記述問題: 数行の修正・穴埋めにより実際に使えるかを確認する

候補が単純な場合は、LLMが問題数を2問に減らしてよい。

初期に扱う問題:

- 説明問題: 「ここで `await` を外すと何が起こるか」
- 因果問題: 「この記法が必要な理由は何か」
- 比較問題: 「`?.` と `??` はこの場面でどう役割が違うか」
- 小さなコード修正: 数行の追加・修正・穴埋め

後続で検討する問題:

- 小さい関数の記述
- 複数の設計案の比較
- ブラウザ上での実行・自動採点

問題は「なぜこう実装したか」だけに寄せず、想起、因果、比較、別場面への応用を段階的に扱う。

## 6. 回答と苦手理由

各回答に任意で苦手理由を付ける。

- 用語が分からない
- JavaScript / TypeScriptの前提が曖昧
- コードの流れが分からない
- 設計意図が分からない
- 理解できた

これは正誤だけでは分からない「次に何を復習すべきか」を判断する材料にする。回答履歴と時間を置いた再回答により、定着の変化を追う。

### 判定とフィードバック

- 選択肢問題は選択結果で判定する
- 自由記述・コード記述は、LLMが問題、PR、コードの根拠に沿って判定する
- 自由記述は模範解答との完全一致ではなく、必要な要点を説明できているかで判定する
- 初期の理解度表示は細かな点数ではなく、`できた`、`一部できた`、`もう一度` を基本とする
- フィードバックでは、できている点、不足点、根拠となるコードやPR情報を短く示す

回答や根拠が不足して理解度を判断できない場合は、不正解ではなく`判定保留`とする。判定保留では、ユーザーは回答の修正、解説の確認、深掘り対話を選べる。

### 問題の根拠表示

- 問題を解くために必要な最小限のコード断片は最初から表示する
- PR本文、差分、レビューコメントなどの詳細は、`この問題の根拠を見る`から確認できる
- 設計判断の問題では、判断理由の根拠となるPR本文、レビューコメント、判断メモも短く表示する
- フィードバック後は、根拠となるコード行、PR本文、レビューコメントを強調表示する
- 根拠には、種別、PR番号、commit SHA、ファイルパス、行範囲、取り込み時点の断片を保存する

### 復習

復習対象は、最初にPRから選んだ学習候補と、その候補に対して生成した問題セットである。技術タグ全体をまとめて復習対象にはしない。

- 初回学習後、3日後、14日後、30日後の計3回を復習予定とする
- 復習では、初回と同じPRスナップショットを根拠に、別の問い方で理解を確かめる
- `できた`場合は次の復習予定へ進む
- `一部できた`場合は次の段階へ進めず、短い間隔で再出題する
- `もう一度`の場合は翌日または次回起動時に再出題する
- `判定保留`は不正解にはせず、回答修正・解説確認・深掘り対話を選べる
- 30日後の復習まで問題なければ、学習セットを`定着済み`にする

アプリを開いた時は、今日が復習予定の学習セットを最大3件表示する。ユーザーは1件を選んで復習でき、新しいPRの学習を選ぶこともできる。

各学習セットには`復習を終了`を設ける。復習予定からは外すが、PR、回答、苦手理由、学習履歴は削除しない。後から`復習を再開`でき、終了直後は`元に戻す`を表示する。

## 7. 深掘り対話

問題画面から、ユーザーが自由なタイミングで「深掘りする」を選べる。回答前、回答途中、フィードバック後のいずれでも利用できる。

初期版では、候補選択時の「今は飛ばす」や回答時の「わからない」を専用ボタンとしては設けない。別候補の選択・回答の自由記述・苦手理由の記録で扱う。

- 現在の問題、回答済みの場合はユーザー回答、該当コード、PR・差分・レビューコメントなどの根拠を起点にLLMと対話する
- 会話の生ログを保存する
- ユーザーが必要な場合だけ、自分の言葉での要点や未解決点を学習記録として保存する
- 深掘り後に問題を自動作成・作り直ししない。復習間隔や理解度も変更しない

通常の問題生成には、深掘り会話の全ログではなく、ユーザーが保存した学習要点だけを使う。

## 8. データとコンテキストの扱い

### 保存対象

- 取り込み時点のPRスナップショット（PR本文、差分、変更ファイル、コミット情報、レビューコメント、追加取得した周辺コード）
- PR URL、取得日時、その時点のhead commit SHA
- ユーザーが許可して取り込んだCursor会話
- アプリ内の深掘り会話
- 問題、回答、苦手理由、復習履歴
- PR・会話から作成した要約、技術テーマ、判断カード

### 圧縮と参照

生ログはアーカイブとして保持できるが、問題生成のたびにすべてをLLMへ送らない。

PRや会話の区切りで、以下を含む学習用の圧縮記録を作る。

- 実装したこと
- つまずきと解消内容
- 未解決の疑問
- 技術的な判断、代替案、制約
- 関連するPR・ファイル・会話箇所
- 復習候補

新しいPRの解析時は、対象ファイル・関数・技術テーマ・判断カードに関連する過去の圧縮記録だけを検索してLLMへ渡す。

```text
PR 1 を解析・圧縮
PR 2 を解析 → PR 1 の関連記録だけ参照
PR 3 を解析 → PR 1・2 の関連記録だけ参照
```

PRは初回取り込み時のスナップショットを教材の正本とする。GitHub上のPRが後から変わっても、過去の問題と復習の根拠は変えない。ユーザーが「最新版へ更新」を実行した場合だけ、新しいスナップショットを取得し、更新日時と対象commit SHAを記録する。

## 9. 設計判断を扱う条件

PR差分だけから、LLMが実装意図を断定してはいけない。

設計判断の問題は、PR説明、レビューコメント、コミット、Cursor会話、ユーザーの判断メモなどに根拠がある場合に限る。根拠が足りないときは、ユーザーに確認するか、設計問題の対象から外す。

差分・コードだけを根拠にする場合は、挙動、型、記法、因果、比較を問う問題を作成する。作者の目的やトレードオフを事実として問う問題は作成しない。

判断カードには以下を保存する。

- 採用した案
- 検討した代替案
- 制約・選定理由
- 根拠へのリンク
- 将来見直す条件

## 10. LLM利用方針

BYOK（Bring Your Own Key）を基本とする。

- MVPで対応するLLMプロバイダーはOpenAI APIのみとする
- ユーザー自身のOpenAI APIキーで問題生成・フィードバック・深掘りを行い、API費用はユーザーのOpenAI契約側で発生する
- APIキーは初回ログイン時には求めず、最初に問題生成を実行するタイミングで入力を求める
- APIキーはブラウザを閉じると消える利用セッションだけで保持する。GoバックエンドはAPI呼び出し時に一時的に受け取るが、DB、ログ、オブジェクトストレージには保存しない
- MVPではモデル選択をユーザーに提供せず、アプリ側で指定した1つのモデルを利用する
- アプリはLLMの会話履歴を自動では読めないため、学習履歴の正本はアプリ側で管理する
- ChatGPTやCursorの画面上の過去会話を、API経由で勝手に読むことはしない

問題生成の前に、送信するPR内容と任意で添付したCursor会話を、ユーザーのOpenAI APIキーでOpenAIへ送信することを明示し、ユーザーが実行を選んでから送信する。

LLM呼び出しでは、用途に応じて必要最小限のデータだけを送信する。

- 学習候補・問題生成: PRスナップショット、必要最小限の周辺コード、任意でアップロードしたCursor会話
- 回答の判定・フィードバック: 現在の問題、ユーザー回答、問題の根拠となるコード・PR情報
- 深掘り対話: 現在の問題、回答済みの場合は回答、該当コード、PRの根拠、深掘り会話の直近文脈
- 復習問題の生成: 初回に保存したPRスナップショット、選んだ学習候補、過去の回答結果

別のPRの全文、関係ないCursor会話、他の学習セットの回答全文、アプリ内の全履歴は送信しない。

APIキーが無効、利用上限・残高不足、一時的な通信エラー、LLM出力の形式不正を区別して表示する。失敗時もPRデータや回答履歴は削除せず、再入力または再試行できるようにする。

## 11. 保存基盤の方針

- PostgreSQL: ユーザー、PR、問題、回答、要約、関連付けなど検索・表示に必要な構造化データ
- オブジェクトストレージ: 長い生会話、PRスナップショット、取り込みファイルなど大きな本文

ユーザーは生ログの閲覧・エクスポート・削除を行える。

## 12. 公開初期の範囲

公開時は「他のユーザーが安全に試せる」設計にするが、大量アクセス対応までは行わない。

含める:

- GitHub OAuthによるログイン。用途はユーザー識別と学習履歴の保存であり、非公開リポジトリ権限は要求しない
- ユーザーごとのデータ分離
- トークンの安全な保管
- 連携解除・データ削除
- 公開PRのURLによる取り込み、任意のCursor会話Markdownアップロード、問題生成、回答保存、後日の復習
- 利用上限

後回しにする:

- 無制限の一般登録
- 大量同時解析のスケール対策
- 複雑なジョブキュー・自動スケール
- Cursorの完全自動連携
- 開発セッション中の自動収集・事前解析
- ブラウザ上のコード実行環境
- 課金・チーム管理
- GitHub Appを使った非公開リポジトリ・非公開PRの取り込み
- 用語辞書

公開方法は、招待制ベータまたはデモモードを想定する。ログインなしで公開PRを試せる閲覧用デモも検討する。

## 13. 技術構成の仮説

- フロントエンド: React + TypeScript
- バックエンド: Go
- 認証: GitHub OAuth（本人識別・学習履歴の保存用。非公開リポジトリ権限は要求しない）
- PR取得: GitHub API（公開PRのURLを入力して取得）
- データベース: PostgreSQL
- 大きな生ログ: オブジェクトストレージ
- AI: ユーザーBYOKのLLM API、構造化出力で問題・要約・判断カードを作成

Goを選ぶ理由は、GitHub連携、LLM呼び出し、後続の非同期処理・定期処理を安定して扱いやすいため。データ量そのものではなく、取得・解析・再試行を分けて運用する設計を重視する。

## 14. 未決定事項

- 最初に対応するLLMプロバイダー
- APIキーをサーバー保存するか、セッション限定にするか
- 復習間隔と出題優先度のルール
- 開発セッション機能をいつ導入するか（開始・終了、PR更新の収集、問題の事前生成）
- 公開初期の登録方法（招待制、申請制、デモのみ）
- 非公開PR対応の必要性と、必要になった場合のGitHub App連携・LLM送信範囲
